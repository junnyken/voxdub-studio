# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# Virtualenvs (tao boi cac script trong scripts/)
.venv/
.venv-*/
venv/

# Cau hinh ca nhan — chep .env.example thanh .env roi dien
.env

# Ket qua chay pipeline
output/
downloads/
batch_state.json
logs/

# Du lieu ca nhan
linkdouyin.txt

# Models (nhieu GB; tai ve bang cac script trong scripts/) — CHI thu muc
# models/ o GOC repo (autodub tai ve: models/whisper, models/vieneu,...).
# Khong duoc de "models/" khong neo (khop moi thu muc ten "models" o BAT KY
# do sau nao) — tung lam control_server/src/models/ (schema Mongoose that,
# can commit) bi bo lot khoi git hoan toan, xem docs/TEST_LOG.md.
/models/

# PoC lip-sync (mini-spec V32a, docs/PLAN.md Phase G) — repo MuseTalk clone
# ve THAT (khong phai submodule) + model weights (~5-6GB) + video/report ket
# qua benchmark ca nhan tung may. scripts/research/lipsync_poc.py (harness
# THAT cua VoxDub) VAN duoc commit binh thuong, chi rieng 2 thu muc con nay
# bi loai.
/scripts/research/musetalk_repo/
/scripts/research/lipsync_poc_output/

# Giong nguoi dung tu them (voices/preset_voices_vn/ VAN duoc commit)
voices/custom/
voices/*.wav

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

# Build exe
*.spec.bak
pw-browsers/
smoke_test_result.json
smoke_err.txt

# Backend (chay tren may chu rieng)
control_server/node_modules/
control_server/.env
control_server/logs/

# Satellite deploy override (server B, workspace ca nhan) — tu file tuyen bo
# khong commit, xem comment trong chinh file do.
docker-compose.b-satellite.yml

# Website
website/node_modules/
website/dist/
